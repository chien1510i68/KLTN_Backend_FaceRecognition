
spring:
  main:
    allow-bean-definition-overriding: true
  datasource:
    url: jdbc:mysql://localhost:3306/attendance
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect


myconfig:
  passwordHash: jwt-demo@6868
jwt:
  jwtSecret: jwt@6868
  jwtExpirationMs: 30000000
  jwtRefreshExpirationMs: 360000000

server:
  port: 8080




